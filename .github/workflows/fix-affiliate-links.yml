name: Fix AffiliateBase Links
on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Fix affiliate links
        env:
          AFFILIATEBASE_API_KEY: ${{ secrets.AFFILIATEBASE_API_KEY }}
        run: node scripts/fix-affiliate-links.mjs

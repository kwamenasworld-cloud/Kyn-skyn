<script>
  // The Why Kyn page is retired. Its brand story lives on /pages/about now;
  // send old links there, keeping any query params.
  window.location.replace('/pages/about' + window.location.search);
</script>

<style>
  .why-redirect {
    background: var(--cream);
    min-height: 60vh;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 8rem 1.5rem 4rem;
  }

  .why-redirect__inner {
    max-width: 32rem;
  }

  .why-redirect__heading {
    font-family: var(--font-serif);
    font-size: clamp(1.8rem, 4vw, 2.6rem);
    font-weight: 300;
    color: var(--maroon);
    line-height: 1.2;
    margin: 0 0 1.25rem;
  }

  .why-redirect__link {
    font-family: var(--font-sans);
    font-size: 0.95rem;
    color: var(--maroon);
    text-decoration: underline;
    text-underline-offset: 3px;
  }
</style>

<!-- No-JS fallback: visible notice with a plain link to the About page. -->
<section class="why-redirect">
  <div class="why-redirect__inner">
    <h1 class="why-redirect__heading">Get to know Kyn Skyn</h1>
    <a href="/pages/about" class="why-redirect__link">About Kyn Skyn</a>
  </div>
</section>

{% schema %}
{
  "name": "Why Kyn Skyn",
  "class": "why-kyn-section",
  "settings": []
}
{% endschema %}

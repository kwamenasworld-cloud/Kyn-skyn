<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "MedicalBusiness",
  "name": "Kyn Skyn Consultation",
  "url": "https://kynskyn.com/pages/consultation",
  "logo": "https:{{ 'logomark.svg' | asset_url }}",
  "description": "Online telehealth consultations for fungal-prone skin conditions. HIPAA-compliant async consultations with licensed providers who diagnose and prescribe for seborrheic dermatitis, fungal acne (Malassezia folliculitis), dandruff, and tinea versicolor.",
  "medicalSpecialty": "Dermatologic",
  "areaServed": { "@type": "Country", "name": "United States" },
  "availableService": [
    {
      "@type": "MedicalTherapy",
      "name": "Seborrheic dermatitis consultation",
      "relevantSpecialty": "Dermatologic"
    },
    {
      "@type": "MedicalTherapy",
      "name": "Fungal acne (Malassezia folliculitis) consultation",
      "relevantSpecialty": "Dermatologic"
    },
    {
      "@type": "MedicalTherapy",
      "name": "Tinea versicolor consultation",
      "relevantSpecialty": "Dermatologic"
    }
  ]
}
</script>

<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "Service",
  "serviceType": "Dermatology telehealth consultation",
  "name": "Fungal-skin telehealth consultation",
  "description": "Async online consultation with a licensed provider for fungal-prone skin conditions. Includes diagnosis and a prescription sent to your pharmacy. Monthly, bimonthly, quarterly, and one-time plans available.",
  "provider": {
    "@type": "MedicalBusiness",
    "name": "Kyn Skyn",
    "url": "https://kynskyn.com"
  },
  "audience": { "@type": "PeopleAudience", "suggestedMinAge": 18 },
  "areaServed": { "@type": "Country", "name": "United States" },
  "offers": [
    { "@type": "Offer", "name": "Monthly Treatment Plan", "price": "59.99", "priceCurrency": "USD", "availability": "https://schema.org/InStock", "url": "https://kynskyn.com/pages/consultation?plan=monthly" },
    { "@type": "Offer", "name": "Every 2 Months Treatment Plan", "price": "69.99", "priceCurrency": "USD", "availability": "https://schema.org/InStock", "url": "https://kynskyn.com/pages/consultation?plan=bimonthly" },
    { "@type": "Offer", "name": "Quarterly Treatment Plan", "price": "149", "priceCurrency": "USD", "availability": "https://schema.org/InStock", "url": "https://kynskyn.com/pages/consultation?plan=quarterly" },
    { "@type": "Offer", "name": "One-time Consult", "price": "79", "priceCurrency": "USD", "availability": "https://schema.org/InStock", "url": "https://kynskyn.com/pages/consultation?plan=one-time" }
  ]
}
</script>

<style>
  /* ========== TELEHEALTH HERO ========== */
  .th-hero {
    background: var(--dark);
    padding: 10rem 3rem 5rem;
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  .th-hero__molecule {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 40rem;
    height: 40rem;
    opacity: 0.04;
    pointer-events: none;
  }

  .th-hero__eyebrow {
    font-family: var(--font-accent);
    font-size: 0.75rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--cream);
    opacity: 0.5;
    margin-bottom: 1.5rem;
  }

  .th-hero__heading {
    font-family: var(--font-serif);
    font-size: clamp(2.2rem, 5vw, 3.8rem);
    font-weight: 300;
    color: var(--cream);
    line-height: 1.15;
    max-width: 48rem;
    margin: 0 auto 1.5rem;
  }

  .th-hero__desc {
    font-family: var(--font-sans);
    font-size: 1rem;
    color: var(--cream);
    opacity: 0.6;
    line-height: 1.7;
    max-width: 36rem;
    margin: 0 auto 2.5rem;
  }

  .th-hero__cta {
    display: inline-block;
    font-family: var(--font-sans);
    font-size: 0.85rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--cream);
    background: var(--button);
    padding: 1rem 2.5rem;
    border-radius: 2px;
    text-decoration: none;
    transition: background 0.3s;
  }

  .th-hero__cta:hover { background: var(--maroon); }

  /* ========== CONDITIONS ========== */
  .th-conditions {
    padding: 6rem 3rem;
    background: var(--cream);
  }

  .th-conditions__container {
    max-width: 64rem;
    margin: 0 auto;
    text-align: center;
  }

  .th-conditions__eyebrow {
    font-family: var(--font-accent);
    font-size: 0.75rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--maroon);
    opacity: 0.5;
    margin-bottom: 1.5rem;
  }

  .th-conditions__heading {
    font-family: var(--font-serif);
    font-size: clamp(1.8rem, 4vw, 2.8rem);
    font-weight: 300;
    color: var(--maroon);
    margin-bottom: 3.5rem;
  }

  .th-conditions__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
  }

  .th-card {
    background: var(--dark);
    border-radius: 6px;
    padding: 2.5rem 2rem;
    text-align: left;
    position: relative;
    overflow: hidden;
  }

  .th-card__icon {
    width: 2.5rem;
    height: 2.5rem;
    color: var(--gold);
    margin-bottom: 1.5rem;
  }

  .th-card__title {
    font-family: var(--font-serif);
    font-size: 1.4rem;
    font-weight: 300;
    color: var(--cream);
    margin-bottom: 0.75rem;
  }

  .th-card__desc {
    font-family: var(--font-sans);
    font-size: 0.85rem;
    color: var(--cream);
    opacity: 0.6;
    line-height: 1.7;
    margin-bottom: 1.5rem;
  }

  .th-card__symptoms {
    list-style: none;
  }

  .th-card__symptoms li {
    font-family: var(--font-sans);
    font-size: 0.8rem;
    color: var(--cream);
    opacity: 0.55;
    padding: 0.35rem 0;
    border-top: 1px solid rgba(255, 244, 206, 0.08);
  }

  .th-card__symptoms li:last-child {
    border-bottom: 1px solid rgba(255, 244, 206, 0.08);
  }

  /* ========== HOW IT WORKS ========== */
  .th-steps {
    padding: 6rem 3rem;
    background: var(--dark);
  }

  .th-steps__container {
    max-width: 64rem;
    margin: 0 auto;
    text-align: center;
  }

  .th-steps__eyebrow {
    font-family: var(--font-accent);
    font-size: 0.75rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--cream);
    opacity: 0.5;
    margin-bottom: 1.5rem;
  }

  .th-steps__heading {
    font-family: var(--font-serif);
    font-size: clamp(1.8rem, 4vw, 2.8rem);
    font-weight: 300;
    color: var(--cream);
    margin-bottom: 4rem;
  }

  .th-steps__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2rem;
    text-align: left;
  }

  .th-step {
    position: relative;
  }

  .th-step__num {
    font-family: var(--font-serif);
    font-size: 3rem;
    font-weight: 300;
    color: var(--gold);
    opacity: 0.3;
    line-height: 1;
    margin-bottom: 1rem;
  }

  .th-step__title {
    font-family: var(--font-sans);
    font-size: 0.95rem;
    font-weight: 600;
    color: var(--cream);
    margin-bottom: 0.5rem;
  }

  .th-step__desc {
    font-family: var(--font-sans);
    font-size: 0.8rem;
    color: var(--cream);
    opacity: 0.5;
    line-height: 1.7;
  }

  /* ========== CTA SECTION ========== */
  .th-cta {
    padding: 6rem 3rem;
    background: var(--cream);
    text-align: center;
  }

  .th-cta--dark {
    background: var(--dark) !important;
  }
  .th-cta--dark .th-cta__heading,
  .th-cta--dark .th-cta__desc {
    color: var(--cream) !important;
  }
  .th-cta--dark .th-cta__desc {
    opacity: 0.8;
  }

  .th-cta__container {
    max-width: 40rem;
    margin: 0 auto;
  }

  .th-cta__heading {
    font-family: var(--font-serif);
    font-size: clamp(1.8rem, 4vw, 2.8rem);
    font-weight: 300;
    color: var(--maroon);
    margin-bottom: 1rem;
  }

  .th-cta__desc {
    font-family: var(--font-sans);
    font-size: 1rem;
    color: var(--maroon);
    opacity: 0.6;
    line-height: 1.7;
    margin-bottom: 2.5rem;
  }

  .th-cta__button {
    display: inline-block;
    font-family: var(--font-sans);
    font-size: 0.85rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--cream);
    background: var(--button);
    padding: 1rem 2.5rem;
    border-radius: 2px;
    text-decoration: none;
    transition: background 0.3s;
  }

  .th-cta__button:hover { background: var(--maroon); }

  .th-cta__note {
    font-family: var(--font-sans);
    font-size: 0.75rem;
    color: var(--maroon);
    opacity: 0.4;
    margin-top: 1.5rem;
    line-height: 1.6;
  }

  /* ========== FAQ ========== */
  .th-faq {
    padding: 6rem 3rem;
    background: var(--dark);
  }

  .th-faq__container {
    max-width: 48rem;
    margin: 0 auto;
  }

  .th-faq__heading {
    font-family: var(--font-serif);
    font-size: clamp(1.8rem, 4vw, 2.8rem);
    font-weight: 300;
    color: var(--cream);
    text-align: center;
    margin-bottom: 3rem;
  }

  .th-faq__item {
    border-bottom: 1px solid rgba(255, 244, 206, 0.1);
    padding: 1.5rem 0;
  }

  .th-faq__q {
    font-family: var(--font-sans);
    font-size: 0.95rem;
    font-weight: 600;
    color: var(--cream);
    cursor: pointer;
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: none;
    border: none;
    width: 100%;
    text-align: left;
    padding: 0;
  }

  .th-faq__q::after {
    content: '+';
    font-size: 1.2rem;
    color: var(--gold);
    flex-shrink: 0;
    margin-left: 1rem;
    transition: transform 0.3s;
  }

  .th-faq__q.active::after {
    content: '\2212';
  }

  .th-faq__a {
    font-family: var(--font-sans);
    font-size: 0.85rem;
    color: var(--cream);
    opacity: 0.6;
    line-height: 1.7;
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.3s ease, padding 0.3s ease;
  }

  .th-faq__a.open {
    max-height: 20rem;
    padding-top: 1rem;
  }

  .th-faq__a p { margin: 0; }
  .th-faq__a p + p { margin-top: 0.8rem; }
  .th-faq__a a { color: inherit; text-decoration: underline; }

  /* ========== RESPONSIVE ========== */
  @media (max-width: 768px) {
    .th-hero { padding: 8rem 1.5rem 3rem; }
    .th-conditions { padding: 4rem 1.5rem; }
    .th-conditions__grid { grid-template-columns: 1fr; }
    .th-steps { padding: 4rem 1.5rem; }
    .th-steps__grid { grid-template-columns: 1fr 1fr; gap: 2.5rem; }
    .th-cta { padding: 4rem 1.5rem; }
    .th-faq { padding: 4rem 1.5rem; }
  }

  @media (max-width: 480px) {
    .th-steps__grid { grid-template-columns: 1fr; }
  }

  /* ========== PROVIDERS ========== */
  .th-providers {
    padding: 5rem 3rem;
    background: var(--cream);
  }

  .th-providers__container {
    max-width: 64rem;
    margin: 0 auto;
    text-align: center;
  }

  .th-providers__eyebrow {
    font-family: var(--font-accent);
    font-size: 0.75rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--maroon);
    opacity: 0.6;
    margin-bottom: 1rem;
  }

  .th-providers__heading {
    font-family: var(--font-serif);
    font-size: clamp(1.8rem, 3.5vw, 2.6rem);
    font-weight: 300;
    color: var(--maroon);
    line-height: 1.2;
    max-width: 40rem;
    margin: 0 auto 1rem;
  }

  .th-providers__sub {
    font-family: var(--font-sans);
    font-size: 1rem;
    color: var(--maroon);
    opacity: 0.78;
    line-height: 1.6;
    max-width: 40rem;
    margin: 0 auto 3rem;
  }

  .th-providers__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
    text-align: left;
  }

  .th-vetting-card {
    padding: 2rem;
    background: #fff;
    border: 1px solid rgba(100, 18, 32, 0.1);
    border-radius: 8px;
  }

  .th-vetting-card__icon {
    width: 2.25rem;
    height: 2.25rem;
    color: var(--maroon);
    margin-bottom: 1rem;
  }

  .th-vetting-card__icon svg { width: 100%; height: 100%; }

  .th-vetting-card__title {
    font-family: var(--font-serif);
    font-size: 1.15rem;
    font-weight: 400;
    color: var(--maroon);
    margin: 0 0 0.6rem 0;
  }

  .th-vetting-card__desc {
    font-family: var(--font-sans);
    font-size: 0.9rem;
    color: var(--dark);
    opacity: 0.78;
    line-height: 1.6;
    margin: 0;
  }

  .th-providers__note {
    font-family: var(--font-sans);
    font-size: 0.85rem;
    color: var(--maroon);
    opacity: 0.55;
    margin-top: 2.5rem;
    font-style: italic;
  }

  @media (max-width: 768px) {
    .th-providers__grid { grid-template-columns: 1fr; }
  }

  @media (max-width: 480px) {
    .th-providers { padding: 4rem 1.5rem; }
  }
</style>

<!-- ====== HERO ====== -->
<section class="th-hero">
  <img src="{{ 'molecule.svg' | asset_url }}" alt="" aria-hidden="true" class="th-hero__molecule">
  <p class="th-hero__eyebrow fade-in">Opening July 1, 2026 · Waitlist open</p>
  <h1 class="th-hero__heading fade-in">Get diagnosed by a<br>skin expert.</h1>
  <p class="th-hero__desc fade-in">Kyn Skyn Rx opens July 1. Online consultations with licensed providers who actually treat Malassezia conditions, prescriptions included when you need one. Join the waitlist now for $20 off your first quarterly plan.</p>
  <a href="#th-waitlist" class="th-hero__cta fade-in">Join the waitlist</a>
</section>

<!-- ====== CONDITIONS ====== -->
<section class="th-conditions" id="th-conditions">
  <div class="th-conditions__container">
    <p class="th-conditions__eyebrow fade-in">Conditions</p>
    <h2 class="th-conditions__heading fade-in">What our providers treat</h2>

    <div class="th-conditions__grid">
      <div class="th-card fade-in">
        <svg class="th-card__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
          <circle cx="12" cy="12" r="3"/><circle cx="12" cy="5" r="1.5"/><circle cx="17" cy="8" r="1.5"/><circle cx="17" cy="16" r="1.5"/><circle cx="12" cy="19" r="1.5"/><circle cx="7" cy="16" r="1.5"/><circle cx="7" cy="8" r="1.5"/>
        </svg>
        <h3 class="th-card__title">Fungal Acne</h3>
        <p class="th-card__desc">Malassezia folliculitis. It looks like acne, but acne treatments don't touch it.</p>
        <ul class="th-card__symptoms">
          <li>Uniform small bumps on forehead, chest, or back</li>
          <li>Itchy, especially when sweating</li>
          <li>Doesn't respond to typical acne treatment</li>
          <li>No blackheads or whiteheads</li>
        </ul>
      </div>

      <div class="th-card fade-in">
        <svg class="th-card__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
          <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2z"/><path d="M8 12s1.5 2 4 2 4-2 4-2"/><path d="M9 8.5c0-.28.22-.5.5-.5s.5.22.5.5"/><path d="M14 8.5c0-.28.22-.5.5-.5s.5.22.5.5"/>
        </svg>
        <h3 class="th-card__title">Seborrheic Dermatitis</h3>
        <p class="th-card__desc">Flaking and redness in the oiliest parts of your face and scalp. People often treat it as dry skin and it gets worse.</p>
        <ul class="th-card__symptoms">
          <li>Flaky, scaly patches on scalp, eyebrows, or nose</li>
          <li>Greasy or powdery scale</li>
          <li>Redness that comes and goes</li>
          <li>Worse with stress, weather changes, or certain products</li>
        </ul>
      </div>

      <div class="th-card fade-in">
        <svg class="th-card__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
          <ellipse cx="12" cy="12" rx="8" ry="10"/><path d="M8 9c1-1 3-1 4 0"/><path d="M12 9c1-1 3-1 4 0"/><path d="M7 15c2 1 4 1 5 0"/><path d="M12 15c2 1 4 1 5 0"/>
        </svg>
        <h3 class="th-card__title">Tinea Versicolor</h3>
        <p class="th-card__desc">Patchy discoloration on your chest, back, or shoulders when Malassezia overgrows on the skin.</p>
        <ul class="th-card__symptoms">
          <li>Light or dark patches on chest, back, or shoulders</li>
          <li>Fine, powdery scale when scratched</li>
          <li>More visible after sun exposure</li>
          <li>Patches that don't tan evenly</li>
        </ul>
      </div>
    </div>
  </div>
</section>

<!-- ====== HOW IT WORKS ====== -->
<section class="th-steps">
  <div class="th-steps__container">
    <p class="th-steps__eyebrow fade-in">Process</p>
    <h2 class="th-steps__heading fade-in">How it works</h2>

    <div class="th-steps__grid">
      <div class="th-step fade-in">
        <div class="th-step__num">01</div>
        <h3 class="th-step__title">Pick your concern</h3>
        <p class="th-step__desc">Tell us what you're dealing with. Not sure? A provider can help you figure it out.</p>
      </div>

      <div class="th-step fade-in">
        <div class="th-step__num">02</div>
        <h3 class="th-step__title">Submit your intake</h3>
        <p class="th-step__desc">Share your history and photos of your skin. It takes about 10 minutes — no scheduling required.</p>
      </div>

      <div class="th-step fade-in">
        <div class="th-step__num">03</div>
        <h3 class="th-step__title">Get diagnosed</h3>
        <p class="th-step__desc">A licensed provider reviews everything and sends you a diagnosis and plan, usually within 24 hours.</p>
      </div>

      <div class="th-step fade-in">
        <div class="th-step__num">04</div>
        <h3 class="th-step__title">Start treatment</h3>
        <p class="th-step__desc">You leave with a plan. If there's a prescription, it's already at your pharmacy.</p>
      </div>
    </div>
  </div>
</section>

<!-- ====== PROVIDERS ====== -->
<section class="th-providers" id="th-providers">
  <div class="th-providers__container">
    <p class="th-providers__eyebrow fade-in">Your care team</p>
    <h2 class="th-providers__heading fade-in">Vetted, licensed, and trained on Malassezia.</h2>
    <p class="th-providers__sub fade-in">Most clinicians see Malassezia conditions for a few minutes a year. Ours specialize in them — including how they present on melanated skin, where they're routinely missed.</p>

    <div class="th-providers__grid">
      <div class="th-vetting-card fade-in">
        <div class="th-vetting-card__icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M9 12l2 2 4-4"/><circle cx="12" cy="12" r="10"/></svg>
        </div>
        <h3 class="th-vetting-card__title">Licensed in your state</h3>
        <p class="th-vetting-card__desc">Every provider is an MD, DO, or NP, verified through the NPI registry. We can only treat patients in states where your provider holds an active license.</p>
      </div>
      <div class="th-vetting-card fade-in">
        <div class="th-vetting-card__icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 6.253v13M12 6.253C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253M12 6.253C13.168 5.477 14.754 5 16.5 5s3.332.477 4.5 1.253v13C19.832 18.477 18.246 18 16.5 18s-3.332.477-4.5 1.253"/></svg>
        </div>
        <h3 class="th-vetting-card__title">Malassezia-trained</h3>
        <p class="th-vetting-card__desc">Each provider completes our briefing on fungal acne, seb derm, and tinea versicolor, including how these conditions present on Black, brown, and Asian skin.</p>
      </div>
      <div class="th-vetting-card fade-in">
        <div class="th-vetting-card__icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0110 0v4"/></svg>
        </div>
        <h3 class="th-vetting-card__title">HIPAA-compliant care</h3>
        <p class="th-vetting-card__desc">Your medical records are encrypted at rest and in transit, kept separate from your shop history, and never shared with third parties.</p>
      </div>
    </div>

    <p class="th-providers__note fade-in">Individual provider profiles publish at launch on July 1, 2026.</p>
  </div>
</section>

<!-- ====== PRICING ====== -->
<section class="th-cta th-cta--dark">
  <div class="th-cta__container fade-in" style="max-width: 500px;">
    <h2 class="th-cta__heading">Choose your plan</h2>
    <div class="th-pricing__grid" role="radiogroup" aria-label="Choose your plan">
      <!-- Monthly — $59.99 (with quarterly billing toggle as an augment) -->
      <button type="button" class="th-plan th-plan--selected" data-plan="monthly" role="radio" aria-checked="true">
        <span class="th-plan__badge th-plan__badge--amber">Recommended</span>
        <p class="th-plan__price" data-price="treatment_monthly">$59.99<span class="th-plan__price-suffix">/mo</span></p>
        <p class="th-plan__label">Monthly</p>
        <div class="th-plan__features">
          <p>&#10003; Provider consultation</p>
          <p>&#10003; Rx sent to your pharmacy</p>
          <p>&#10003; Monthly adjustments</p>
          <p>&#10003; Cancel anytime</p>
        </div>
        <!-- Quarterly billing toggle — augments the Monthly plan -->
        <div class="th-plan__augment" aria-hidden="true">
          <label class="th-plan__augment-toggle">
            <input type="checkbox" class="th-plan__augment-input" data-augment="quarterly">
            <span class="th-plan__augment-label">
              <span class="th-plan__augment-title">Pay quarterly, save ~$10/mo</span>
              <span class="th-plan__augment-detail"><span data-price="treatment_quarterly">$149<span class="th-plan__price-suffix">/qtr</span></span> · billed every 3 months</span>
            </span>
          </label>
        </div>
      </button>
      <!-- Every 2 months — $69.99 -->
      <button type="button" class="th-plan" data-plan="bimonthly" role="radio" aria-checked="false">
        <p class="th-plan__price" data-price="treatment_bimonthly">$69.99<span class="th-plan__price-suffix">/2mo</span></p>
        <p class="th-plan__label">Every 2 months · $35/mo effective</p>
        <div class="th-plan__features">
          <p>&#10003; Rx sent to pharmacy every 2 months</p>
          <p>&#10003; One shipment every 2 months</p>
          <p>&#10003; Billed every 2 months</p>
          <p>&#10003; Cancel anytime</p>
        </div>
      </button>
      <!-- One-time Consult — $79 -->
      <button type="button" class="th-plan" data-plan="one-time" role="radio" aria-checked="false">
        <p class="th-plan__price" data-price="treatment_onetime">$79</p>
        <p class="th-plan__label">One-time Consult</p>
        <div class="th-plan__features">
          <p>&#10003; Provider consultation</p>
          <p>&#10003; Rx to your pharmacy</p>
          <p>&#10003; Single visit, no commitment</p>
          <p>&#10003; No recurring charges</p>
        </div>
      </button>
    </div>
    <p class="th-cta__desc" style="font-size: 0.85rem;">Every plan includes a provider consultation and a prescription sent to your pharmacy. You pay the pharmacy for the medication itself. Care runs through a HIPAA-compliant platform with licensed providers.</p>
    <a href="#th-waitlist" class="th-cta__button" id="thPricingCta" style="margin-top: 1.5rem;">Join the waitlist — $20 off quarterly</a>
  </div>
</section>

<style>
  .th-pricing__grid {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 0.8rem;
    margin: 2rem 0;
    align-items: start;
  }
  @media (max-width: 640px) {
    .th-pricing__grid { grid-template-columns: 1fr; }
  }
  .th-plan {
    background: rgba(255, 244, 206, 0.04);
    border: 2px solid rgba(255, 244, 206, 0.15);
    border-radius: 16px;
    padding: 1.3rem;
    text-align: center;
    position: relative;
    color: inherit;
    font: inherit;
    cursor: pointer;
    outline: none;
    transition: border-color 0.2s, background 0.2s, transform 0.15s;
  }
  .th-plan:hover { border-color: rgba(255, 244, 206, 0.35); }
  .th-plan:focus-visible { box-shadow: 0 0 0 3px rgba(248, 176, 68, 0.4); }
  .th-plan--selected {
    border-color: #F8B044;
    background: rgba(248, 176, 68, 0.18);
  }
  .th-plan__badge {
    position: absolute;
    top: -0.6rem;
    right: 0.8rem;
    font-family: var(--font-sans);
    font-size: 0.55rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    padding: 0.25rem 0.6rem;
    border-radius: 100px;
  }
  .th-plan__badge--amber { background: #F8B044; color: #641220; }
  .th-plan__badge--teal { background: #055A5B; color: #C5FEFF; }
  .th-plan__price {
    font-family: var(--font-accent);
    font-size: 2rem;
    color: var(--cream);
    margin: 0;
    transition: color 0.2s;
  }
  .th-plan--selected .th-plan__price { color: #F8B044; }
  .th-plan__price-suffix { font-size: 0.9rem; opacity: 0.6; }
  .th-plan__label {
    font-family: var(--font-sans);
    font-size: 0.85rem;
    color: var(--cream);
    opacity: 0.75;
    margin-top: 0.3rem;
  }
  .th-plan__features {
    margin-top: 0.8rem;
    font-family: var(--font-sans);
    font-size: 0.7rem;
    color: rgba(255, 244, 206, 0.55);
    text-align: left;
    line-height: 1.7;
  }
  .th-plan__features p { margin: 0; }
  /* Quarterly augment — sits inside the Monthly card, below the feature list */
  .th-plan__augment {
    margin-top: 0.9rem;
    padding-top: 0.8rem;
    border-top: 1px solid rgba(255, 244, 206, 0.12);
    text-align: left;
  }
  .th-plan__augment-toggle {
    display: flex;
    align-items: flex-start;
    gap: 0.55rem;
    cursor: pointer;
    font-family: var(--font-sans);
  }
  .th-plan__augment-input {
    appearance: none;
    -webkit-appearance: none;
    margin: 0.15rem 0 0 0;
    width: 0.9rem;
    height: 0.9rem;
    border: 1.5px solid rgba(255, 244, 206, 0.4);
    border-radius: 3px;
    background: transparent;
    cursor: pointer;
    flex-shrink: 0;
    position: relative;
    transition: background 0.15s, border-color 0.15s;
  }
  .th-plan__augment-input:checked {
    background: #F8B044;
    border-color: #F8B044;
  }
  .th-plan__augment-input:checked::after {
    content: "";
    position: absolute;
    left: 3px;
    top: 0px;
    width: 3px;
    height: 6px;
    border: solid #641220;
    border-width: 0 2px 2px 0;
    transform: rotate(45deg);
  }
  .th-plan__augment-label { display: block; }
  .th-plan__augment-title {
    display: block;
    font-size: 0.7rem;
    font-weight: 600;
    color: #F8B044;
    letter-spacing: 0.02em;
  }
  .th-plan__augment-detail {
    display: block;
    font-size: 0.65rem;
    color: rgba(255, 244, 206, 0.55);
    margin-top: 0.1rem;
  }

  /* ── Waitlist form (on cream section background) ── */
  .th-waitlist__eyebrow {
    display: inline-block;
    font-family: var(--font-accent, 'STK Bureau Sans', sans-serif);
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--maroon);
    background: rgba(248, 176, 68, 0.18);
    border: 1px solid rgba(100, 18, 32, 0.25);
    padding: 0.35rem 0.8rem;
    border-radius: 999px;
    margin-bottom: 1.2rem;
  }
  .th-waitlist__form {
    display: flex;
    gap: 0.5rem;
    max-width: 480px;
    margin: 1.8rem auto 0.5rem;
    flex-wrap: wrap;
  }
  .th-waitlist__input {
    flex: 1;
    min-width: 220px;
    padding: 0.9rem 1.1rem;
    font-family: var(--font-sans, 'STK Bureau Sans', sans-serif);
    font-size: 0.95rem;
    background: #ffffff;
    border: 1px solid rgba(100, 18, 32, 0.3);
    border-radius: 4px;
    color: var(--dark);
  }
  .th-waitlist__input::placeholder { color: rgba(61, 32, 37, 0.4); }
  .th-waitlist__input:focus { outline: none; border-color: var(--maroon); box-shadow: 0 0 0 3px rgba(100, 18, 32, 0.08); }
  .th-waitlist__button {
    padding: 0.9rem 1.8rem;
    font-family: var(--font-sans, 'STK Bureau Sans', sans-serif);
    font-size: 0.8rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--cream);
    background: var(--maroon);
    border: 1px solid var(--maroon);
    border-radius: 4px;
    cursor: pointer;
    transition: background 0.2s, border-color 0.2s;
  }
  .th-waitlist__button:hover { background: var(--dark); border-color: var(--dark); }
  .th-waitlist__button:disabled { opacity: 0.6; cursor: not-allowed; }
  .th-waitlist__note {
    font-family: var(--font-sans, 'STK Bureau Sans', sans-serif);
    font-size: 0.9rem;
    margin: 0.6rem 0 0;
    min-height: 1.2rem;
    color: var(--dark);
  }
  .th-waitlist__note:empty { display: none; }
  @media (max-width: 540px) {
    .th-waitlist__form { flex-direction: column; }
    .th-waitlist__input { width: 100%; }
    .th-waitlist__button { width: 100%; }
  }
</style>

<script>
  (function() {
    var pricingCta = document.getElementById('thPricingCta');
    var plans = document.querySelectorAll('.th-plan');
    var augmentInput = document.querySelector('.th-plan__augment-input[data-augment="quarterly"]');
    var CTA_LABELS = {
      'monthly': 'Continue with Monthly · $[price]/mo',
      'bimonthly': 'Continue every 2 months · $[price]/2mo',
      'quarterly': 'Continue with Quarterly · $[price]/qtr',
      'one-time': 'Book One-time Consult · $[price]'
    };

    function readPrice(selector) {
      var el = document.querySelector(selector);
      if (!el) return '';
      var clone = el.cloneNode(true);
      var suffix = clone.querySelector('.th-plan__price-suffix');
      if (suffix) suffix.parentNode.removeChild(suffix);
      return clone.textContent.replace(/[^0-9.]/g, '');
    }

    // Determine effective plan: selected card, overridden to quarterly if Monthly + augment checkbox
    function getEffectivePlan() {
      var selected = document.querySelector('.th-plan.th-plan--selected');
      var plan = selected ? selected.getAttribute('data-plan') : 'monthly';
      if (plan === 'monthly' && augmentInput && augmentInput.checked) return 'quarterly';
      return plan;
    }

    function setPricingCta() {
      // No-op while the page is in pre-launch waitlist mode.
      // The CTA stays as a static scroll link to #th-waitlist.
    }

    plans.forEach(function(plan) {
      plan.addEventListener('click', function(e) {
        // Don't hijack clicks inside the augment toggle label
        if (e.target && e.target.closest && e.target.closest('.th-plan__augment')) return;
        plans.forEach(function(p) {
          p.classList.remove('th-plan--selected');
          p.setAttribute('aria-checked', 'false');
        });
        plan.classList.add('th-plan--selected');
        plan.setAttribute('aria-checked', 'true');
        // Switching to a non-Monthly card clears the quarterly augment
        if (augmentInput && plan.getAttribute('data-plan') !== 'monthly') {
          augmentInput.checked = false;
        }
        setPricingCta();
      });
    });

    if (augmentInput) {
      augmentInput.addEventListener('change', function() {
        // Turning on the augment forces the Monthly card to be selected
        if (augmentInput.checked) {
          plans.forEach(function(p) {
            var isMonthly = p.getAttribute('data-plan') === 'monthly';
            p.classList.toggle('th-plan--selected', isMonthly);
            p.setAttribute('aria-checked', isMonthly ? 'true' : 'false');
          });
        }
        setPricingCta();
      });
    }
  })();

  // ── Waitlist form submission ──
  (function () {
    var form = document.getElementById('thWaitlistForm');
    if (!form) return;
    var input = form.querySelector('input[type="email"]');
    var btn = form.querySelector('.th-waitlist__button');
    var note = document.getElementById('thWaitlistNote');

    form.addEventListener('submit', async function (e) {
      e.preventDefault();
      var email = input.value.trim();
      if (!email) return;

      btn.disabled = true;
      btn.textContent = 'Joining...';
      note.textContent = '';

      try {
        var formData = new FormData();
        formData.append('EMAIL', email);
        formData.append('SOURCE', 'consultation-waitlist-quarterly-20off');
        formData.append('b_61338503f23a719d2d581c864_051996d936', '');

        await fetch('https://kynskyn.us15.list-manage.com/subscribe/post?u=61338503f23a719d2d581c864&id=051996d936', {
          method: 'POST',
          body: formData,
          mode: 'no-cors'
        });

        btn.textContent = "You're on the list";
        btn.style.background = '#2a6e3f';
        btn.style.borderColor = '#2a6e3f';
        btn.style.color = '#ffffff';
        note.textContent = "We'll email you on July 1 with your $20-off code.";
        note.style.color = '#2a6e3f';
      } catch (err) {
        btn.disabled = false;
        btn.textContent = 'Join the waitlist';
        note.textContent = 'Something went wrong — please try again.';
        note.style.color = '#b02a2a';
      }
    });
  })();
</script>


<!-- ====== WAITLIST ====== -->
<section class="th-cta" id="th-waitlist">
  <div class="th-cta__container fade-in">
    <p class="th-waitlist__eyebrow">Opening July 1, 2026</p>
    <h2 class="th-cta__heading">Be first in line.</h2>
    <p class="th-cta__desc">Join the Kyn Skyn Rx waitlist and we'll email you the moment booking opens. Waitlist members get <strong>$20 off their first quarterly plan</strong> — $149 becomes $129, and that includes provider consultation plus Rx sent to your pharmacy.</p>
    <form class="th-waitlist__form" id="thWaitlistForm">
      <input type="email" class="th-waitlist__input" placeholder="you@email.com" required aria-label="Email address">
      <button type="submit" class="th-waitlist__button">Join the waitlist</button>
    </form>
    <p class="th-waitlist__note" id="thWaitlistNote"></p>
    <p class="th-cta__note">One email when we launch. No spam. Unsubscribe anytime.</p>
  </div>
</section>

<!-- ====== FAQ ====== -->
{% assign faq_blocks = section.blocks | where: 'type', 'faq' %}

{% if faq_blocks.size > 0 %}
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [
    {%- for block in faq_blocks -%}
      {
        "@type": "Question",
        "name": {{ block.settings.question | json }},
        "acceptedAnswer": {
          "@type": "Answer",
          "text": {{ block.settings.answer | strip_html | json }}
        }
      }{% unless forloop.last %},{% endunless %}
    {%- endfor -%}
  ]
}
</script>
{% endif %}

<section class="th-faq">
  <div class="th-faq__container">
    <h2 class="th-faq__heading fade-in">Common questions</h2>

    {% for block in faq_blocks %}
      <div class="th-faq__item fade-in" {{ block.shopify_attributes }}>
        <button class="th-faq__q" type="button">{{ block.settings.question }}</button>
        <div class="th-faq__a"{% if block.settings.enable_dynamic_pricing %} data-price="faq_pricing"{% endif %}>{{ block.settings.answer }}</div>
      </div>
    {% endfor %}
  </div>
</section>

<script>
  document.querySelectorAll('.th-faq__q').forEach(function(btn) {
    btn.addEventListener('click', function() {
      var answer = this.nextElementSibling;
      var isOpen = answer.classList.contains('open');
      document.querySelectorAll('.th-faq__a').forEach(function(a) { a.classList.remove('open'); });
      document.querySelectorAll('.th-faq__q').forEach(function(q) { q.classList.remove('active'); });
      if (!isOpen) {
        answer.classList.add('open');
        this.classList.add('active');
      }
    });
  });

  // Dynamic pricing from Supabase — single source of truth
  (function() {
    var SUPABASE_URL = 'https://pbriwsgraemyqhkymneq.supabase.co';
    var SUPABASE_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InBicml3c2dyYWVteXFoa3ltbmVxIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzUwNTQxNjEsImV4cCI6MjA5MDYzMDE2MX0.AFfCsWNblX3MzjOJVRL7JefLzaLQrsc6xHSeDZuFWrI';

    fetch(SUPABASE_URL + '/rest/v1/pricing_config?select=key,value', {
      headers: { 'apikey': SUPABASE_KEY }
    })
    .then(function(r) { return r.json(); })
    .then(function(prices) {
      var p = {};
      // Preserve decimals — don't round $59.99 to $60
      prices.forEach(function(row) {
        var n = Number(row.value);
        // Strip trailing .00 but keep decimals like .99
        p[row.key] = (n % 1 === 0) ? String(Math.round(n)) : n.toFixed(2).replace(/\.?0+$/, '');
      });

      // Update plan card prices (uses CSS class for suffix styling)
      var monthlyEl = document.querySelector('[data-price="treatment_monthly"]');
      if (monthlyEl && p.treatment_monthly) {
        monthlyEl.innerHTML = '$' + p.treatment_monthly + '<span class="th-plan__price-suffix">/mo</span>';
      }
      var bimonthlyEl = document.querySelector('[data-price="treatment_bimonthly"]');
      if (bimonthlyEl && p.treatment_bimonthly) {
        bimonthlyEl.innerHTML = '$' + p.treatment_bimonthly + '<span class="th-plan__price-suffix">/2mo</span>';
      }
      var qtrEl = document.querySelector('[data-price="treatment_quarterly"]');
      if (qtrEl && p.treatment_quarterly) {
        qtrEl.innerHTML = '$' + p.treatment_quarterly + '<span class="th-plan__price-suffix">/qtr</span>';
      }
      var otEl = document.querySelector('[data-price="treatment_onetime"]');
      if (otEl && p.treatment_onetime) {
        otEl.textContent = '$' + p.treatment_onetime;
      }

      // Update FAQ pricing text
      var faqEl = document.querySelector('[data-price="faq_pricing"]');
      if (faqEl && p.treatment_monthly && p.treatment_onetime && p.treatment_quarterly && p.treatment_bimonthly) {
        faqEl.textContent = 'Treatment Plans are $' + p.treatment_monthly + '/month, $' + p.treatment_bimonthly + ' every 2 months, or $' + p.treatment_quarterly + ' quarterly. All include your provider consultation and prescription sent to your pharmacy. A one-time consult is $' + p.treatment_onetime + '. Payment runs securely through Stripe.';
      }
    })
    .catch(function() { /* keep hardcoded defaults */ });
  })();
</script>

{% schema %}
{
  "name": "Consultation",
  "class": "consultation-section",
  "blocks": [
    {
      "type": "faq",
      "name": "FAQ Item",
      "settings": [
        {
          "type": "text",
          "id": "question",
          "label": "Question"
        },
        {
          "type": "richtext",
          "id": "answer",
          "label": "Answer"
        },
        {
          "type": "checkbox",
          "id": "enable_dynamic_pricing",
          "label": "Auto-update with live pricing",
          "default": false,
          "info": "Replaces the answer text at runtime with current pricing from the database. Only enable on the pricing FAQ."
        }
      ]
    }
  ]
}
{% endschema %}

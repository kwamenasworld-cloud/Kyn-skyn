<script>
  // The standalone medication guides are retired. Their education lives in the
  // Malassezia Handbook; send old links there, keeping any query params.
  window.location.replace('/pages/handbook' + window.location.search);
</script>

<style>
  .tg-redirect {
    background: var(--cream);
    min-height: 60vh;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 8rem 1.5rem 4rem;
  }

  .tg-redirect__inner {
    max-width: 32rem;
  }

  .tg-redirect__heading {
    font-family: var(--font-serif);
    font-size: clamp(1.8rem, 4vw, 2.6rem);
    font-weight: 300;
    color: var(--maroon);
    line-height: 1.2;
    margin: 0 0 1.25rem;
  }

  .tg-redirect__link {
    font-family: var(--font-sans);
    font-size: 0.95rem;
    color: var(--maroon);
    text-decoration: underline;
    text-underline-offset: 3px;
  }
</style>

<!-- No-JS fallback: visible notice with a plain link to the Handbook. -->
<section class="tg-redirect">
  <div class="tg-redirect__inner">
    <h1 class="tg-redirect__heading">This guide now lives in the Malassezia Handbook</h1>
    <a href="/pages/handbook" class="tg-redirect__link">Read the Handbook</a>
  </div>
</section>

{% schema %}
{
  "name": "Treatment Guide",
  "class": "treatment-guide-section",
  "settings": []
}
{% endschema %}
